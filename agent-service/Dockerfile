FROM python:3.12-slim
WORKDIR /app
COPY requirements.lock .
RUN pip install --no-cache-dir -r requirements.lock
COPY phonease phonease
COPY tests tests
EXPOSE 8000
CMD ["uvicorn", "phonease.app:app", "--host", "0.0.0.0", "--port", "8000", "--no-access-log"]
